# **`const`和`define`的区别**
## **`const`用于定义常量，`define`用于定义宏，也能定义常量**
<br/>

## **区别：**
1.  ### **`const`生效于编译阶段，`define`生效于预处理阶段**
2.  ### **`const`定义的常量存储在内存中，需要额外的内存；`define`定义的常量是运行时直接操作，不会存放在内存中**
3.  ### **`const`定义的常量是带类型的；`define`定义的常量不带类型。因为`define`定义的常量不利于类型检查**