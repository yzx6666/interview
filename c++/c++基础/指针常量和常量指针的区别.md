# **常量指针和指针常量**

1.  **```int const *a```指针常量、，指针指向的内存的值不变，即```(*a)```不变**
2.  **```int *const a**```常量指针，指针指向的内存的地址不变，即```a```不变**

## **其他类型**
```c++
1. const int a;     //指的是a是一个常量，不允许修改。
2. const int *a;    //同 int const *a;
3. const int *const a;   //都不变，即（*a）不变，a也不变
```