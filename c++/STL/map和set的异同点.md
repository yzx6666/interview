# map和set的异同点
<br/>

## 相同点：
1. **都是以红黑树作为底层容器的**
2. **不允许出现键值重复**
3. **所有的元素都是自动排序的（map根据键值排序）**
4. **都是关联型容器**

## 不同点：
1. **set只有key没有value，key就是value**<br/>**map都有**
2. **set不能通过迭代器修改值，因为set的值就是键。**<br/>
   **map的键不能修改，但是键对应的值可以修改**