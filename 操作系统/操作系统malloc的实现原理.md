# 操作系统中malloc

## malloc底层实现原理：
### 当开辟的空间小于128k时，调用brk()函数；当开辟空间大于128k时，调用mmap()。malloc采用内存池的管理方式，以减少内存碎片。先申请大内存作为堆区，然后将对区分为多个内存块。当用户申请内存时，直接从堆中分配一块合适的空闲块。malloc采用`隐式链表`将所有的空闲块连接起来，每一个空闲块记录一个未分配的、连续的内存地址。