# 32位系统能访问4GB以上的内存吗?

## `正常情况下是不可以的。` 原因是计算机使用的是二进制，每位数只有0或1两个状态，32wei正好是2的32次方，正好是4G，所以大于4G就没办法表示了，而32位系统中，因其它原因还需要占用一部分空间，所以内存只有3G多，要使用4G以上就只能换64位操作系统。

## 但是PAE技术可以实现32位系统访问4G以上内存`（PAE技术将地址扩展到了36位，这样，系统就能够容纳2^36=64GB的内存。）`