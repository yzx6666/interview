# **Linux 内核态和用户态**

## **内核态和用户态**
### 内核态与用户态是操作系统的两种运行级别。内核态拥有最高权限，可以访问所有系统指令；用户则只能调用一部分指令；

<br/>

## **什么时候进入内核态：**
1.  ### **系统调用(主动)**
2.  ### **异常(被动)**
3.  ### **设备中断(被动)**

<br/>

## **为什么区分用户态和内核态**
### **在CPU的所有指令中，有一些指令是非常危险的，如果错用，将导致整个系统的崩溃。比如：清内存、设置时钟等。所以区分用户态和内核态主要出于安全的考虑。**

